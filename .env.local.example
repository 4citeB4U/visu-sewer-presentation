# -----------------------------------------------------------------------------
# Agent Lee / Presentation TTS Environment Configuration (Vite)
# -----------------------------------------------------------------------------
# Copy this file to `.env.local` (DO NOT COMMIT REAL SECRETS) and fill in values.
# Vite only exposes variables prefixed with VITE_.
# -----------------------------------------------------------------------------
# Azure Cognitive Services Text-to-Speech
# Obtain a key & region from: https://portal.azure.com -> Cognitive Services -> Speech
VITE_AZURE_TTS_KEY=YOUR_AZURE_TTS_KEY_HERE
VITE_AZURE_TTS_REGION=eastus
# Common voices: en-US-JennyNeural, en-US-GuyNeural, en-US-AriaNeural
VITE_AZURE_TTS_VOICE=en-US-JennyNeural
# Style examples: general | newscast | cheerful | sad | angry | fearful | serious | whispering
VITE_AZURE_TTS_STYLE=general

# Gemini TTS (Google Generative Language API)
# Enable the Generative Language API and create an API key at:
# https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
# Recommended preview model supporting TTS (adjust if Google changes naming)
VITE_GEMINI_MODEL=gemini-2.5-flash-preview-tts
# Prebuilt voice examples: Kore, Alto, Wave (subject to change)
VITE_GEMINI_VOICE=Kore

# Optional: Force default engine at startup (browser | azure | gemini)
# Leave unset to start with browser and fall back automatically.
VITE_TTS_DEFAULT_ENGINE=browser

# Optional: Increase logging verbosity for TTS diagnostics
VITE_DEBUG_TTS=1

# Optional: App port override (used when running `npx vite --port`)
VITE_PORT=3000

# OpenRouter (LLM via OpenAI-compatible API)
# Create key: https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=YOUR_OPENROUTER_API_KEY_HERE
# Default model: Google Gemma 3n 2B (free)
VITE_OPENROUTER_MODEL=google/gemma-3n-e2b-it:free
# Optional for OpenRouter rankings (your site/app identity)
VITE_SITE_URL=http://localhost:3000
VITE_SITE_TITLE=VisuSewer Agent Lee

# -----------------------------------------------------------------------------
# SECURITY / GIT IGNORES
#   1. Rename this file to `.env.local` for local use.
#   2. Ensure `.gitignore` contains `.env.local` and patterns like `.env*`.
#   3. NEVER commit real production keys.
# -----------------------------------------------------------------------------
# TESTING STEPS AFTER SETUP:
#   1. Copy -> rename to .env.local and fill keys.
#   2. Restart dev server: `npm run dev`.
#   3. Open the Chatbot voice settings, select Azure or Gemini engine.
#   4. Click "Test Audio"; you should hear speech. If browser fails you should
#      still hear cloud audio or the fallback beep if keys missing.
# -----------------------------------------------------------------------------
